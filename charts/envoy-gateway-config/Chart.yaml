apiVersion: v2
name: envoy-gateway-config
description: One Envoy Gateway ingress tier (GatewayClass, EnvoyProxy, Gateway, traffic policies, monitors); rendered once for internal and once for external
type: application
version: 1.0.0
appVersion: "1.0"
