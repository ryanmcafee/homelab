{{- if index .Values "cloudnative-pg" "enabled" }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cloudnative-pg
  namespace: argocd
  annotations:
    # Sync wave 10 - after kube-prometheus-stack (wave 9), whose PodMonitor CRD the
    # operator's podMonitorEnabled needs; cnpg-barman-cloud follows at 11
    argocd.argoproj.io/sync-wave: "10"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: {{ index .Values "cloudnative-pg" "chart" "repo" }}
    chart: {{ index .Values "cloudnative-pg" "chart" "name" }}
    targetRevision: {{ index .Values "cloudnative-pg" "chart" "version" }}
    helm:
      releaseName: cloudnative-pg
      values: |
        {{- with index .Values "cloudnative-pg" "replicaCount" }}
        replicaCount: {{ . }}
        {{- end }}

        {{- if index .Values "cloudnative-pg" "resources" }}
        resources:
          {{- toYaml (index .Values "cloudnative-pg" "resources") | nindent 10 }}
        {{- end }}

        {{- if index .Values "cloudnative-pg" "monitoring" }}
        monitoring:
          {{- toYaml (index .Values "cloudnative-pg" "monitoring") | nindent 10 }}
        {{- end }}

  destination:
    server: https://kubernetes.default.svc
    namespace: {{ index .Values "cloudnative-pg" "namespace" }}

  syncPolicy:
    {{- if .Values.global.automatedSync }}
    automated:
      prune: true
      selfHeal: true
    {{- end }}
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - ApplyOutOfSyncOnly=true
      - RespectIgnoreDifferences=true

  # Ignore differences in webhook configurations
  ignoreDifferences:
    - group: admissionregistration.k8s.io
      kind: ValidatingWebhookConfiguration
      jsonPointers:
        - /webhooks/0/clientConfig/caBundle
        - /webhooks/1/clientConfig/caBundle
    - group: admissionregistration.k8s.io
      kind: MutatingWebhookConfiguration
      jsonPointers:
        - /webhooks/0/clientConfig/caBundle
{{- end }}
