{{- if index .Values "external-dns" "enabled" }}
{{- if eq (index .Values "external-dns" "provider") "cloudflare" }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: external-dns-cloudflare
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "4"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: {{ index .Values "external-dns" "chart" "repo" }}
    chart: {{ index .Values "external-dns" "chart" "name" }}
    targetRevision: {{ index .Values "external-dns" "chart" "version" }}
    helm:
      releaseName: external-dns-cloudflare
      values: |
        fullnameOverride: external-dns-cloudflare

        provider:
          name: {{ index .Values "external-dns" "provider" }}

        {{- if eq (index .Values "external-dns" "provider") "cloudflare" }}
        env:
          - name: CF_API_TOKEN
            valueFrom:
              secretKeyRef:
                name: cloudflare-api-token
                key: api_token

        cloudflare:
          proxied: {{ index .Values "external-dns" "cloudflare" "proxied" }}
        {{- end }}

        # HTTPRoutes only (the CRD source has its own deployment)
        sources:
          - gateway-httproute

        domainFilters:
          {{- range (index .Values "external-dns" "domainFilters") }}
          - {{ . }}
          {{- end }}

        annotationFilter: {{ index .Values "external-dns" "annotationFilter" }}

        policy: sync
        registry: txt
        txtOwnerId: {{ .Values.global.environment }}-cloudflare

        # Only routes on the external Gateway (e.g. Plex); internal routes
        # publish to UniFi, never to Cloudflare
        extraArgs:
          - --gateway-name={{ .Values.global.gateway.external }}
          - --gateway-namespace={{ .Values.global.gateway.namespace }}
        {{- if index .Values "external-dns" "defaultTarget" }}
          - --default-targets={{ index .Values "external-dns" "defaultTarget" }}
        {{- end }}

        resources:
          {{- toYaml (index .Values "external-dns" "resources") | nindent 10 }}

  destination:
    server: https://kubernetes.default.svc
    namespace: {{ index .Values "external-dns" "namespace" }}

  syncPolicy:
    {{- if .Values.global.automatedSync }}
    automated:
      prune: true
      selfHeal: true
    {{- end }}
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - ApplyOutOfSyncOnly=true
{{- end }}
---
# Config Application: deploys OnePasswordItems for API credentials before external-dns
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: external-dns-config
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "3"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: {{ .Values.global.repoUrl }}
    targetRevision: {{ .Values.global.targetRevision }}
    path: charts/external-dns-config
    helm:
      valueFiles:
        - values.yaml
        - values-{{ .Values.global.environment }}.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: external-dns
  syncPolicy:
    {{- if .Values.global.automatedSync }}
    automated:
      prune: true
      selfHeal: true
    {{- end }}
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
{{- end }}
