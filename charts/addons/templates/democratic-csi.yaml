{{- if index .Values "democratic-csi" "enabled" }}
---
# Namespace with privileged PodSecurity for CSI driver requirements
# (hostNetwork, hostIPC, SYS_ADMIN capabilities, privileged containers)
apiVersion: v1
kind: Namespace
metadata:
  name: {{ index .Values "democratic-csi" "namespace" }}
  labels:
    pod-security.kubernetes.io/enforce: privileged
    pod-security.kubernetes.io/audit: privileged
    pod-security.kubernetes.io/warn: privileged
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: democratic-csi
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "2"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: {{ index .Values "democratic-csi" "chart" "repo" }}
    chart: {{ index .Values "democratic-csi" "chart" "name" }}
    targetRevision: {{ index .Values "democratic-csi" "chart" "version" }}
    helm:
      releaseName: democratic-csi
      values: |
        csiDriver:
          name: "org.democratic-csi.nfs"

        driver:
          config:
            driver: {{ index .Values "democratic-csi" "driver" }}

            httpConnection:
              protocol: https
              host: {{ index .Values "democratic-csi" "truenas" "endpoint" | trimPrefix "https://" | trimPrefix "http://" }}
              port: 443
              allowInsecure: {{ index .Values "democratic-csi" "truenas" "allowInsecure" }}
              apiKey: "${TRUENAS_API_KEY}"

            zfs:
              datasetParentName: {{ index .Values "democratic-csi" "nfs" "parent" }}
              # Convention: snapshots dataset = parent + "-snapshots" (e.g. storage/k8s -> storage/k8s-snapshots)
              detachedSnapshotsDatasetParentName: {{ index .Values "democratic-csi" "nfs" "parent" }}-snapshots
              datasetEnableQuotas: true
              datasetEnableReservation: false
              datasetPermissionsMode: "0770"
              datasetPermissionsUser: 0
              datasetPermissionsGroup: 0

            nfs:
              shareHost: {{ index .Values "democratic-csi" "nfs" "shareHost" }}
              shareAlldirs: false
              shareAllowedHosts: []
              shareAllowedNetworks:
                - {{ index .Values "democratic-csi" "nfs" "shareAllow" }}
              shareMapallUser: root
              shareMapallGroup: root

        storageClasses:
        {{- range (index .Values "democratic-csi" "storageClasses") }}
          - name: {{ .name }}
            defaultClass: {{ .defaultClass }}
            reclaimPolicy: {{ .reclaimPolicy }}
            volumeBindingMode: {{ .volumeBindingMode }}
            allowVolumeExpansion: {{ .allowVolumeExpansion }}
            parameters:
              fsType: {{ .parameters.fsType }}
            mountOptions:
              - nfsvers=4
              - noatime
        {{- end }}

        controller:
          driver:
            resources:
              {{- toYaml (index .Values "democratic-csi" "resources" "controller") | nindent 14 }}
            extraEnv:
              - name: TRUENAS_API_KEY
                valueFrom:
                  secretKeyRef:
                    name: truenas-api-key
                    key: api_key

        node:
          driver:
            resources:
              {{- toYaml (index .Values "democratic-csi" "resources" "node") | nindent 14 }}
            extraEnv:
              - name: TRUENAS_API_KEY
                valueFrom:
                  secretKeyRef:
                    name: truenas-api-key
                    key: api_key

  destination:
    server: https://kubernetes.default.svc
    namespace: {{ index .Values "democratic-csi" "namespace" }}

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
      - ApplyOutOfSyncOnly=true
---
# Config Application: deploys OnePasswordItem for TrueNAS API key before democratic-csi
# Must deploy after 1Password operator (wave -1) but before democratic-csi (wave 2)
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: democratic-csi-config
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: {{ .Values.global.repoUrl }}
    targetRevision: {{ .Values.global.targetRevision }}
    path: charts/democratic-csi-config
    helm:
      valueFiles:
        - values.yaml
        - values-{{ .Values.global.environment }}.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: democratic-csi
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
{{- end }}
