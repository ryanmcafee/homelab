{{- /* Picked up by the kube-prometheus-stack Grafana sidecar (label grafana_dashboard, any namespace). */}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: clickhouse-logs-dashboards
  namespace: {{ .Values.namespace }}
  labels:
    grafana_dashboard: "1"
data:
  {{- range $path, $_ := .Files.Glob "dashboards/*.json" }}
  {{ base $path }}: |-
    {{- $.Files.Get $path | replace "__NETWORK_NAMES__" $.Values.networkNames | nindent 4 }}
  {{- end }}
