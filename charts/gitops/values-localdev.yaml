# GitOps Umbrella Chart - Local Development Overrides
# Phase 0.5: Local Development Environment
#
# This file provides overrides for local development with Kind cluster.
# It points to the localdev-specific values for both addons and applications.

# ============================================================================
# Global Configuration
# ============================================================================

global:
  # Environment identifier
  environment: localdev

  # Must match configuration/environments/localdev.yaml's DOMAIN (config is
  # the source of truth). Currently unused by any template in this chart,
  # but kept in sync to avoid misleading drift if that changes.
  domain: homelab.local

  # Repository URL - can be local path or GitHub URL
  # ArgoCD will pull from this repository
  repoUrl: https://github.com/ryanmcafee/homelab.git

  # Target branch for local development
  targetRevision: HEAD

# ============================================================================
# Bootstrap Application (Prerequisites)
# ============================================================================

bootstrap:
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false

# ============================================================================
# Addons Application
# ============================================================================

addons:
  enabled: true

  source:
    # Use localdev-specific values
    valueFiles:
      - values.yaml
      - values-localdev.yaml

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false

# ============================================================================
# Applications
# ============================================================================

applications:
  enabled: true

  source:
    # Use localdev-specific values
    valueFiles:
      - values.yaml
      - values-localdev.yaml

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
