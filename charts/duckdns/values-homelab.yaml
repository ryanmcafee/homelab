# duckdns homelab values

namespace: duckdns

# The DuckDNS subdomain is not set here: the parent Application
# (charts/applications/templates/duckdns.yaml) passes DUCKDNS_SUBDOMAIN from
# configuration/ via helm.valuesObject.
