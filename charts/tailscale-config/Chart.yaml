apiVersion: v2
name: tailscale-config
description: CRD-dependent configuration for the Tailscale operator (operator-oauth Secret via OnePasswordItem)
type: application
version: 1.0.0
appVersion: "1.0"
