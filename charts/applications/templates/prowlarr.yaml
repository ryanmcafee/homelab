{{- if include "homelab.preview.appEnabled" (dict "root" . "app" "prowlarr") }}
{{- if not (include "homelab.preview.enabled" .) }}
---
# Prowlarr Config: deploys static PV+PVC for prowlarr persistent storage
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: prowlarr-config
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "12"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: {{ .Values.global.repoUrl }}
    targetRevision: {{ .Values.global.targetRevision }}
    path: charts/prowlarr-config
    helm:
      valueFiles:
        - values.yaml
        - values-{{ .Values.global.environment }}.yaml
      valuesObject:
        iscsi:
          portal: {{ .Values.global.iscsi.portal | quote }}
  destination:
    server: https://kubernetes.default.svc
    namespace: {{ .Values.prowlarr.namespace }}
  syncPolicy:
    {{- if .Values.global.automatedSync }}
    automated:
      prune: true
      selfHeal: true
    {{- end }}
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - SkipDryRunOnMissingResource=true
{{- end }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ include "homelab.preview.appName" (dict "root" . "app" "prowlarr") }}
  namespace: {{ include "homelab.preview.appNamespace" . }}
  annotations:
    argocd.argoproj.io/sync-wave: "13"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: {{ include "homelab.preview.project" . }}
  source:
    repoURL: oci.trueforge.org/truecharts
    chart: {{ .Values.prowlarr.chart.name }}
    targetRevision: {{ .Values.prowlarr.chart.version }}
    helm:
      releaseName: prowlarr
      values: |
        {{- if .Values.prowlarr.workload }}
        workload:
          {{- toYaml .Values.prowlarr.workload | nindent 10 }}
        {{- end }}

        {{- if .Values.prowlarr.persistence }}
        persistence:
          {{- include "homelab.preview.persistence" (dict "root" . "persistence" .Values.prowlarr.persistence) | nindent 10 }}
        {{- end }}

        {{- if .Values.prowlarr.route }}
        route:
          {{- include "homelab.preview.hosts" (dict "root" . "text" (toYaml .Values.prowlarr.route)) | nindent 10 }}
        {{- end }}

        {{- if .Values.prowlarr.TZ }}
        TZ: {{ .Values.prowlarr.TZ | quote }}
        {{- end }}

        {{- if .Values.prowlarr.securityContext }}
        securityContext:
          {{- toYaml .Values.prowlarr.securityContext | nindent 10 }}
        {{- end }}

  destination:
    server: https://kubernetes.default.svc
    namespace: {{ include "homelab.preview.destNamespace" (dict "root" . "namespace" .Values.prowlarr.namespace) }}

  # Ignore TrueCharts dynamic fields and Kubernetes defaults
  ignoreDifferences:
    - group: apps
      kind: Deployment
      jqPathExpressions:
        - .spec.template.metadata.annotations | to_entries | map(select(.key | startswith("checksum/"))) | from_entries
      jsonPointers:
        - /spec/template/spec/hostUsers

  syncPolicy:
    {{- if .Values.global.automatedSync }}
    automated:
      prune: true
      selfHeal: true
    {{- end }}
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - ApplyOutOfSyncOnly=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
{{- include "homelab.smokeJob" (dict "name" "prowlarr" "namespace" (include "homelab.preview.destNamespace" (dict "root" . "namespace" .Values.prowlarr.namespace)) "smoke" (include "homelab.preview.smoke" (dict "root" . "smoke" .Values.prowlarr.smoke "namespace" .Values.prowlarr.namespace) | fromYaml) "wave" "13" "image" .Values.global.images.curl) }}
{{- end }}
