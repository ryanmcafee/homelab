{{- if include "homelab.preview.appEnabled" (dict "root" . "app" "sonarr") }}
{{- if not (include "homelab.preview.enabled" .) }}
---
# Sonarr Config: deploys static PV+PVC for sonarr persistent storage
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sonarr-config
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "12"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: {{ .Values.global.repoUrl }}
    targetRevision: {{ .Values.global.targetRevision }}
    path: charts/sonarr-config
    helm:
      valueFiles:
        - values.yaml
        - values-{{ .Values.global.environment }}.yaml
      valuesObject:
        iscsi:
          portal: {{ .Values.global.iscsi.portal | quote }}
  destination:
    server: https://kubernetes.default.svc
    namespace: {{ .Values.sonarr.namespace }}
  syncPolicy:
    {{- if .Values.global.automatedSync }}
    automated:
      prune: true
      selfHeal: true
    {{- end }}
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - SkipDryRunOnMissingResource=true
{{- end }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ include "homelab.preview.appName" (dict "root" . "app" "sonarr") }}
  namespace: {{ include "homelab.preview.appNamespace" . }}
  annotations:
    argocd.argoproj.io/sync-wave: "13"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: {{ include "homelab.preview.project" . }}
  source:
    repoURL: oci.trueforge.org/truecharts
    chart: {{ .Values.sonarr.chart.name }}
    targetRevision: {{ .Values.sonarr.chart.version }}
    helm:
      releaseName: sonarr
      values: |
        {{- if .Values.sonarr.workload }}
        workload:
          {{- toYaml .Values.sonarr.workload | nindent 10 }}
        {{- end }}

        {{- if .Values.sonarr.persistence }}
        persistence:
          {{- include "homelab.preview.persistence" (dict "root" . "persistence" .Values.sonarr.persistence) | nindent 10 }}
        {{- end }}

        {{- if .Values.sonarr.route }}
        route:
          {{- include "homelab.preview.hosts" (dict "root" . "text" (toYaml .Values.sonarr.route)) | nindent 10 }}
        {{- end }}

        {{- if .Values.sonarr.TZ }}
        TZ: {{ .Values.sonarr.TZ | quote }}
        {{- end }}

        {{- if .Values.sonarr.securityContext }}
        securityContext:
          {{- toYaml .Values.sonarr.securityContext | nindent 10 }}
        {{- end }}

  destination:
    server: https://kubernetes.default.svc
    namespace: {{ include "homelab.preview.destNamespace" (dict "root" . "namespace" .Values.sonarr.namespace) }}

  # Ignore TrueCharts dynamic fields and Kubernetes defaults
  ignoreDifferences:
    - group: apps
      kind: Deployment
      jqPathExpressions:
        - .spec.template.metadata.annotations | to_entries | map(select(.key | startswith("checksum/"))) | from_entries
      jsonPointers:
        - /spec/template/spec/hostUsers

  syncPolicy:
    {{- if .Values.global.automatedSync }}
    automated:
      prune: true
      selfHeal: true
    {{- end }}
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - ApplyOutOfSyncOnly=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
{{- include "homelab.smokeJob" (dict "name" "sonarr" "namespace" (include "homelab.preview.destNamespace" (dict "root" . "namespace" .Values.sonarr.namespace)) "smoke" (include "homelab.preview.smoke" (dict "root" . "smoke" .Values.sonarr.smoke "namespace" .Values.sonarr.namespace) | fromYaml) "wave" "13" "image" .Values.global.images.curl) }}
{{- end }}
