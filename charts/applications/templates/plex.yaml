{{- /* Never part of a preview (docs/runbooks/previews.md). */ -}}
{{- if and .Values.plex.enabled (not (include "homelab.preview.enabled" .)) }}
---
# Dependencies Application: deploys OnePasswordItem for Plex claim token
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: plex-config
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "11"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: {{ .Values.global.repoUrl }}
    targetRevision: {{ .Values.global.targetRevision }}
    path: charts/plex-config
    helm:
      valueFiles:
        - values.yaml
        - values-{{ .Values.global.environment }}.yaml
      valuesObject:
        iscsi:
          portal: {{ .Values.global.iscsi.portal | quote }}
  destination:
    server: https://kubernetes.default.svc
    namespace: {{ .Values.plex.namespace }}
  syncPolicy:
    {{- if .Values.global.automatedSync }}
    automated:
      prune: true
      selfHeal: true
    {{- end }}
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - SkipDryRunOnMissingResource=true
---
# Plex Application: deploys the media server
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: plex
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "12"
    homelab.ryanmcafee.com/policy-exempt: "image-latest"
    homelab.ryanmcafee.com/policy-exempt-reason: "Upstream plexinc/pms-docker publishes no numbered release tags; tracking :latest is the documented, intentional deployment model for Plex."
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: {{ .Values.plex.chart.repo }}
    chart: {{ .Values.plex.chart.name }}
    targetRevision: {{ .Values.plex.chart.version }}
    helm:
      releaseName: plex
      values: |
        {{- if .Values.plex.image }}
        image:
          {{- toYaml .Values.plex.image | nindent 10 }}
        {{- end }}

        {{- if .Values.plex.extraEnv }}
        extraEnv:
          {{- toYaml .Values.plex.extraEnv | nindent 10 }}
        {{- end }}

        {{- if eq (default "none" .Values.plex.gpu.vendor) "nvidia" }}
        runtimeClassName: {{ .Values.plex.gpu.nvidia.runtimeClassName }}
        {{- end }}

        {{- if and .Values.plex.gpu (ne (default "none" .Values.plex.gpu.vendor) "none") }}
        nodeSelector:
          {{- if eq .Values.plex.gpu.vendor "nvidia" }}
          {{- toYaml .Values.plex.gpu.nvidia.nodeSelector | nindent 10 }}
          {{- else if eq .Values.plex.gpu.vendor "intel" }}
          {{- toYaml .Values.plex.gpu.intel.nodeSelector | nindent 10 }}
          {{- end }}
        {{- end }}

        {{- if .Values.plex.pms }}
        pms:
          {{- toYaml .Values.plex.pms | nindent 10 }}
        {{- end }}

        {{- if or .Values.plex.extraVolumes (eq (default "none" .Values.plex.gpu.vendor) "intel") }}
        extraVolumes:
          {{- if .Values.plex.extraVolumes }}
          {{- toYaml .Values.plex.extraVolumes | nindent 10 }}
          {{- end }}
          {{- if eq (default "none" .Values.plex.gpu.vendor) "intel" }}
          - name: dev-dri
            hostPath:
              path: {{ .Values.plex.gpu.intel.devDri.hostPath }}
              type: Directory
          {{- end }}
        {{- end }}

        {{- if or .Values.plex.extraVolumeMounts (eq (default "none" .Values.plex.gpu.vendor) "intel") }}
        extraVolumeMounts:
          {{- if .Values.plex.extraVolumeMounts }}
          {{- toYaml .Values.plex.extraVolumeMounts | nindent 10 }}
          {{- end }}
          {{- if eq (default "none" .Values.plex.gpu.vendor) "intel" }}
          - name: dev-dri
            mountPath: /dev/dri
          {{- end }}
        {{- end }}

        {{- if .Values.plex.service }}
        service:
          {{- toYaml .Values.plex.service | nindent 10 }}
        {{- end }}

        {{- if .Values.plex.ingress }}
        ingress:
          {{- toYaml .Values.plex.ingress | nindent 10 }}
        {{- end }}

        {{- if .Values.plex.dnsPolicy }}
        dnsPolicy: {{ .Values.plex.dnsPolicy | quote }}
        {{- end }}

        {{- if .Values.plex.dnsConfig }}
        dnsConfig:
          {{- toYaml .Values.plex.dnsConfig | nindent 10 }}
        {{- end }}

  destination:
    server: https://kubernetes.default.svc
    namespace: {{ .Values.plex.namespace }}

  syncPolicy:
    {{- if .Values.global.automatedSync }}
    automated:
      prune: true
      selfHeal: true
    {{- end }}
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - ApplyOutOfSyncOnly=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
{{- include "homelab.smokeJob" (dict "name" "plex" "namespace" .Values.plex.namespace "smoke" .Values.plex.smoke "wave" "12" "image" .Values.global.images.curl) }}
{{- end }}
