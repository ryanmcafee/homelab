{{- if include "homelab.preview.appEnabled" (dict "root" . "app" "radarr") }}
{{- if not (include "homelab.preview.enabled" .) }}
---
# Radarr Config: deploys static PV+PVC for radarr persistent storage
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: radarr-config
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "12"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: {{ .Values.global.repoUrl }}
    targetRevision: {{ .Values.global.targetRevision }}
    path: charts/radarr-config
    helm:
      valueFiles:
        - values.yaml
        - values-{{ .Values.global.environment }}.yaml
      valuesObject:
        iscsi:
          portal: {{ .Values.global.iscsi.portal | quote }}
  destination:
    server: https://kubernetes.default.svc
    namespace: {{ .Values.radarr.namespace }}
  syncPolicy:
    {{- if .Values.global.automatedSync }}
    automated:
      prune: true
      selfHeal: true
    {{- end }}
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - SkipDryRunOnMissingResource=true
{{- end }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ include "homelab.preview.appName" (dict "root" . "app" "radarr") }}
  namespace: {{ include "homelab.preview.appNamespace" . }}
  annotations:
    argocd.argoproj.io/sync-wave: "13"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: {{ include "homelab.preview.project" . }}
  source:
    repoURL: oci.trueforge.org/truecharts
    chart: {{ .Values.radarr.chart.name }}
    targetRevision: {{ .Values.radarr.chart.version }}
    helm:
      releaseName: radarr
      values: |
        {{- if .Values.radarr.workload }}
        workload:
          {{- toYaml .Values.radarr.workload | nindent 10 }}
        {{- end }}

        {{- if .Values.radarr.persistence }}
        persistence:
          {{- include "homelab.preview.persistence" (dict "root" . "persistence" .Values.radarr.persistence) | nindent 10 }}
        {{- end }}

        {{- if .Values.radarr.ingress }}
        ingress:
          {{- include "homelab.preview.hosts" (dict "root" . "text" (toYaml .Values.radarr.ingress)) | nindent 10 }}
        {{- end }}

        {{- if .Values.radarr.TZ }}
        TZ: {{ .Values.radarr.TZ | quote }}
        {{- end }}

        {{- if .Values.radarr.securityContext }}
        securityContext:
          {{- toYaml .Values.radarr.securityContext | nindent 10 }}
        {{- end }}

  destination:
    server: https://kubernetes.default.svc
    namespace: {{ include "homelab.preview.destNamespace" (dict "root" . "namespace" .Values.radarr.namespace) }}

  # Ignore TrueCharts dynamic fields and Kubernetes defaults
  ignoreDifferences:
    - group: apps
      kind: Deployment
      jqPathExpressions:
        - .spec.template.metadata.annotations | to_entries | map(select(.key | startswith("checksum/"))) | from_entries
      jsonPointers:
        - /spec/template/spec/hostUsers

  syncPolicy:
    {{- if .Values.global.automatedSync }}
    automated:
      prune: true
      selfHeal: true
    {{- end }}
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - ApplyOutOfSyncOnly=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
{{- include "homelab.smokeJob" (dict "name" "radarr" "namespace" (include "homelab.preview.destNamespace" (dict "root" . "namespace" .Values.radarr.namespace)) "smoke" (include "homelab.preview.smoke" (dict "root" . "smoke" .Values.radarr.smoke "namespace" .Values.radarr.namespace) | fromYaml) "wave" "13" "image" .Values.global.images.curl) }}
{{- end }}
