{{- if include "homelab.preview.appEnabled" (dict "root" . "app" "nzbget") }}
{{- if not (include "homelab.preview.enabled" .) }}
---
# NZBGet Config: deploys static PV+PVC for nzbget persistent storage
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nzbget-config
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "12"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: {{ .Values.global.repoUrl }}
    targetRevision: {{ .Values.global.targetRevision }}
    path: charts/nzbget-config
    helm:
      valueFiles:
        - values.yaml
        - values-{{ .Values.global.environment }}.yaml
      valuesObject:
        iscsi:
          portal: {{ .Values.global.iscsi.portal | quote }}
  destination:
    server: https://kubernetes.default.svc
    namespace: {{ .Values.nzbget.namespace }}
  syncPolicy:
    {{- if .Values.global.automatedSync }}
    automated:
      prune: true
      selfHeal: true
    {{- end }}
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - SkipDryRunOnMissingResource=true
{{- end }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ include "homelab.preview.appName" (dict "root" . "app" "nzbget") }}
  namespace: {{ include "homelab.preview.appNamespace" . }}
  annotations:
    argocd.argoproj.io/sync-wave: "13"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: {{ include "homelab.preview.project" . }}
  source:
    repoURL: oci.trueforge.org/truecharts
    chart: {{ .Values.nzbget.chart.name }}
    targetRevision: {{ .Values.nzbget.chart.version }}
    helm:
      releaseName: nzbget
      values: |
        {{- if .Values.nzbget.workload }}
        workload:
          {{- toYaml .Values.nzbget.workload | nindent 10 }}
        {{- end }}

        {{- if .Values.nzbget.persistence }}
        persistence:
          {{- include "homelab.preview.persistence" (dict "root" . "persistence" .Values.nzbget.persistence) | nindent 10 }}
        {{- end }}

        {{- if .Values.nzbget.route }}
        route:
          {{- include "homelab.preview.hosts" (dict "root" . "text" (toYaml .Values.nzbget.route)) | nindent 10 }}
        {{- end }}

        {{- if .Values.nzbget.TZ }}
        TZ: {{ .Values.nzbget.TZ | quote }}
        {{- end }}

        {{- if .Values.nzbget.securityContext }}
        securityContext:
          {{- toYaml .Values.nzbget.securityContext | nindent 10 }}
        {{- end }}

  destination:
    server: https://kubernetes.default.svc
    namespace: {{ include "homelab.preview.destNamespace" (dict "root" . "namespace" .Values.nzbget.namespace) }}

  # Ignore TrueCharts dynamic fields and Kubernetes defaults
  ignoreDifferences:
    - group: apps
      kind: Deployment
      jqPathExpressions:
        - .spec.template.metadata.annotations | to_entries | map(select(.key | startswith("checksum/"))) | from_entries
      jsonPointers:
        - /spec/template/spec/hostUsers

  syncPolicy:
    {{- if .Values.global.automatedSync }}
    automated:
      prune: true
      selfHeal: true
    {{- end }}
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - ApplyOutOfSyncOnly=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
{{- include "homelab.smokeJob" (dict "name" "nzbget" "namespace" (include "homelab.preview.destNamespace" (dict "root" . "namespace" .Values.nzbget.namespace)) "smoke" (include "homelab.preview.smoke" (dict "root" . "smoke" .Values.nzbget.smoke "namespace" .Values.nzbget.namespace) | fromYaml) "wave" "13" "image" .Values.global.images.curl) }}
{{- end }}
