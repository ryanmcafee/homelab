{{- if .Values.lazylibrarian.enabled }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: lazylibrarian
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "13"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: oci.trueforge.org/truecharts
    chart: {{ .Values.lazylibrarian.chart.name }}
    targetRevision: {{ .Values.lazylibrarian.chart.version }}
    helm:
      releaseName: lazylibrarian
      values: |
        {{- if .Values.lazylibrarian.workload }}
        workload:
          {{- toYaml .Values.lazylibrarian.workload | nindent 10 }}
        {{- end }}

        {{- if .Values.lazylibrarian.persistence }}
        persistence:
          {{- toYaml .Values.lazylibrarian.persistence | nindent 10 }}
        {{- end }}

        {{- if .Values.lazylibrarian.ingress }}
        ingress:
          {{- toYaml .Values.lazylibrarian.ingress | nindent 10 }}
        {{- end }}

        {{- if .Values.lazylibrarian.TZ }}
        TZ: {{ .Values.lazylibrarian.TZ | quote }}
        {{- end }}

        {{- if .Values.lazylibrarian.securityContext }}
        securityContext:
          {{- toYaml .Values.lazylibrarian.securityContext | nindent 10 }}
        {{- end }}

  destination:
    server: https://kubernetes.default.svc
    namespace: {{ .Values.lazylibrarian.namespace }}

  # Ignore TrueCharts dynamic fields and Kubernetes defaults
  ignoreDifferences:
    - group: apps
      kind: Deployment
      jqPathExpressions:
        - .spec.template.metadata.annotations | to_entries | map(select(.key | startswith("checksum/"))) | from_entries
      jsonPointers:
        - /spec/template/spec/hostUsers

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - ApplyOutOfSyncOnly=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
{{- end }}
