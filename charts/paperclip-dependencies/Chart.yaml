apiVersion: v2
name: paperclip-dependencies
description: Prerequisites for Paperclip (1Password items -> Secrets), synced before the database and the Instance
type: application
version: 1.0.0
appVersion: "1.0"
