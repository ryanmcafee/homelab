{{/*
An OnePasswordItem with an empty itemPath renders but is inert: the operator
has nothing to fetch, so no Secret ever appears while the manifest still reads
like a working producer. Each item therefore requires its own path as well as
the toggle. The 1Password field labels become the Secret keys the Instance
reads (spec.auth.secretRef, spec.auth.adminUser.passwordSecretRef,
spec.adapters.apiKeysSecretRef).
*/}}
{{- if and .Values.onePassword.enabled .Values.onePassword.authItemPath }}
---
apiVersion: onepassword.com/v1
kind: OnePasswordItem
metadata:
  name: paperclip-auth
  namespace: {{ .Values.namespace }}
spec:
  itemPath: {{ .Values.onePassword.authItemPath | quote }}
{{- end }}
{{- if and .Values.onePassword.enabled .Values.onePassword.apiKeysItemPath }}
---
apiVersion: onepassword.com/v1
kind: OnePasswordItem
metadata:
  name: paperclip-api-keys
  namespace: {{ .Values.namespace }}
spec:
  itemPath: {{ .Values.onePassword.apiKeysItemPath | quote }}
{{- end }}
