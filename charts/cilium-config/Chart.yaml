apiVersion: v2
name: cilium-config
description: ServiceMonitors for Cilium, the operator and Hubble, and the Hubble UI ingress (the Cilium release itself is installed by Talos)
type: application
version: 1.0.0
appVersion: "1.0"
