# traefik-internal-config homelab values
#
# The dashboard hostname is not set here. The traefik-internal-config
# Application in charts/addons passes it through helm.valuesObject from
# configuration/ (via the CMP), so this committed file stays PII-free.

namespace: traefik

dashboard:
  enabled: true
