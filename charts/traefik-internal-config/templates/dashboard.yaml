{{- if .Values.dashboard.enabled }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: traefik-internal-dashboard-tls
  namespace: {{ .Values.namespace }}
spec:
  secretName: traefik-internal-dashboard-tls
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
    - {{ .Values.dashboard.host }}
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: internal-dashboard-redirect-slash
  namespace: {{ .Values.namespace }}
spec:
  redirectRegex:
    regex: ^(https?://[^/]+/dashboard)$
    replacement: ${1}/
    permanent: true
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: traefik-internal-dashboard-redirect
  namespace: {{ .Values.namespace }}
  # Both Traefik instances filter the CRD provider by ingress class
  # (--providers.kubernetescrd.ingressClass). Without this annotation neither
  # loads the route: it applies cleanly and then serves the default certificate.
  annotations:
    kubernetes.io/ingress.class: internal
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`{{ .Values.dashboard.host }}`) && Path(`/dashboard`)
      middlewares:
        - name: internal-dashboard-redirect-slash
      services:
        - kind: TraefikService
          name: noop@internal
  tls:
    secretName: traefik-internal-dashboard-tls
{{- end }}
