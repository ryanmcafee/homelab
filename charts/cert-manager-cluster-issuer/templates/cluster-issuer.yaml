{{- if .Values.selfSigned.enabled }}
---
# Self-signed issuer under the letsencrypt name (CERT_ISSUER=selfsigned):
# every cert-manager.io/cluster-issuer: letsencrypt annotation keeps working
# on a cluster without a public domain, and the certificates are untrusted.
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt
spec:
  selfSigned: {}
{{- else if .Values.letsencrypt.enabled }}
---
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt
spec:
  acme:
    server: {{ .Values.letsencrypt.server }}
    email: {{ .Values.letsencrypt.email }}
    privateKeySecretRef:
      name: letsencrypt-account-key
    solvers:
    {{- if .Values.cloudflare.enabled }}
    - dns01:
        cloudflare:
          email: {{ .Values.cloudflare.email }}
          apiTokenSecretRef:
            name: cloudflare-api-token
            key: api_token
        cnameStrategy: Follow
      selector:
        dnsZones:
          {{- range .Values.letsencrypt.dnsZones }}
          - {{ . }}
          {{- end }}
    {{- else }}
    {{- fail "letsencrypt needs cloudflare.enabled: the Gateways serve a wildcard certificate, which only DNS-01 can issue" }}
    {{- end }}
{{- end }}
