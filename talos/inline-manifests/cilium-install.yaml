# Cilium CNI Installation via Talos Inline Manifests
# This manifest is NOT used - Cilium will be installed via ArgoCD
# Keeping this as reference for manual installation if needed

# To install Cilium via Talos inline manifests (alternative to ArgoCD):
# 1. Add this to controlplane.yaml.tpl under cluster.inlineManifests
# 2. Generate the Cilium manifest using Helm template
# 3. Inline it here

# Recommended approach: Install via ArgoCD (charts/addons/cilium.yaml)
# This provides better lifecycle management and GitOps workflow

---
# Example (DO NOT USE - for reference only):
# apiVersion: helm.cattle.io/v1
# kind: HelmChart
# metadata:
#   name: cilium
#   namespace: kube-system
# spec:
#   repo: https://helm.cilium.io/
#   chart: cilium
#   version: 1.14.5
#   targetNamespace: kube-system
#   valuesContent: |-
#     ipam:
#       mode: kubernetes
#     kubeProxyReplacement: strict
#     k8sServiceHost: ${cluster_endpoint}
#     k8sServicePort: 6443
#     hubble:
#       relay:
#         enabled: true
#       ui:
#         enabled: true
