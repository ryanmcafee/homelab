---
# Global variables for all hosts. Addresses, the domain and the timezone come
# from the rendered inventory (configuration/templates/ansible-inventory.tmpl,
# `task ansible:inventory`); only environment-independent settings live here.

# Package Management
apt_cache_valid_time: 3600
apt_upgrade: false  # Set to true for security updates
