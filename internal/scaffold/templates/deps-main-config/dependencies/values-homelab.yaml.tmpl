# [[ .Name ]]-dependencies homelab values. Committed and PII-free (ADR-010):
# 1Password item paths belong here, hostnames and addresses never do.

namespace: [[ .Namespace ]]

onePassword:
  # Set enabled: true and itemPath: vaults/homelab/items/<item> once the item exists.
  enabled: false
