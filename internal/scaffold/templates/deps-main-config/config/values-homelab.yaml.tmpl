# [[ .Name ]]-config homelab values. Committed and PII-free (ADR-010): the
# hostname and domain arrive through the parent Application's helm.valuesObject.
# Only non-derived settings live here.

namespace: [[ .Namespace ]]
