---
phase: 01-hardware-spike-discovery
plan: 04
type: execute
wave: 3
depends_on:
  - "01-03"
files_modified:
  - .planning/phases/01-hardware-spike-discovery/findings/proxmox-ssh-session.log
  - .planning/phases/01-hardware-spike-discovery/findings/spk-02-pci-id.md
  - .planning/phases/01-hardware-spike-discovery/findings/spk-03-iommu-group.md
  - .planning/phases/01-hardware-spike-discovery/findings/spk-04-subsystem-id.md
  - .planning/phases/01-hardware-spike-discovery/findings/spk-05-iommu-cmdline.md
autonomous: false
requirements:
  - SPK-02
  - SPK-03
  - SPK-04
  - SPK-05
tags:
  - hardware-discovery
  - proxmox
  - iommu
  - pci-passthrough
  - wave-b
must_haves:
  truths:
    - "Operator has captured the Intel Arc Pro B50 real PCI bus address from lspci on the live Proxmox host"
    - "Operator has captured the B50 vendor:device ID matching the Intel ARK reference 8086:e212"
    - "Operator has captured the B50 IOMMU group number and the complete list of devices in that group (isolation proof)"
    - "Operator has captured the B50 subsystem ID (SVendor:SDevice) for terragrunt env.hcl population in Phase 5"
    - "Operator has captured the current kernel driver bound to the B50 and documented the Phase 7 binding prep requirement if not vfio-pci"
    - "Operator has confirmed IOMMU is enabled — either intel_iommu=on in /proc/cmdline (Intel) or AMD-Vi active in dmesg (AMD EPYC platform)"
    - "After this plan, task spk:verify exits 0 (all 14 validation rows green)"
  artifacts:
    - path: ".planning/phases/01-hardware-spike-discovery/findings/proxmox-ssh-session.log"
      provides: "Verbatim SSH transcript of task spk:capture — raw source for the 4 per-SPK files"
      contains: "=== SPK-02: B50 PCI ID ==="
    - path: ".planning/phases/01-hardware-spike-discovery/findings/spk-02-pci-id.md"
      provides: "B50 PCI bus address + vendor:device ID captured from live host (Phase 5 env.hcl input)"
      contains: "8086:e212"
    - path: ".planning/phases/01-hardware-spike-discovery/findings/spk-03-iommu-group.md"
      provides: "B50 IOMMU group number + isolation status (Phase 7 passthrough gate)"
      contains: "iommu_group:"
    - path: ".planning/phases/01-hardware-spike-discovery/findings/spk-04-subsystem-id.md"
      provides: "B50 subsystem ID (SVendor:SDevice) + vfio-pci binding state (Phase 5 env.hcl input)"
      contains: "subsystem_id:"
    - path: ".planning/phases/01-hardware-spike-discovery/findings/spk-05-iommu-cmdline.md"
      provides: "Proxmox IOMMU proof — kernel cmdline (Intel) or AMD-Vi dmesg evidence (AMD) for Phase 7 prerequisite"
      contains: "intel_iommu=on|AMD-Vi"
  key_links:
    - from: "findings/spk-02-pci-id.md (pci_bus_address)"
      to: "terragrunt/environments/homelab/env.hcl (Intel device block, Phase 5)"
      via: "manual copy during Phase 5 planning"
      pattern: "pci_bus_address: 0000:"
    - from: "findings/spk-03-iommu-group.md (devices_in_group)"
      to: "Phase 7 HW-08 Plex transcode readiness gate"
      via: "status field: VERIFIED or VERIFIED-WITH-FALLBACK"
      pattern: "iommu_group:"
    - from: "findings/spk-04-subsystem-id.md (subsystem_id)"
      to: "terragrunt/environments/homelab/env.hcl Intel device block (Phase 5)"
      via: "manual copy during Phase 5 planning"
      pattern: "subsystem_id: [0-9a-f]{4}:[0-9a-f]{4}"
    - from: "findings/spk-05-iommu-cmdline.md"
      to: "Phase 7 prerequisites checklist"
      via: "grep -iE 'intel_iommu=on|AMD-Vi.*enabled|AMD-Vi.*Using global'"
      pattern: "intel_iommu=on|AMD-Vi"
---

<objective>
Run `task spk:capture` against the live Proxmox host `root@proxmox.ryanmcafee.com`
and author four per-SPK findings files (SPK-02, SPK-03, SPK-04, SPK-05) from the
captured transcript, then confirm `task spk:verify` exits 0.

This is Wave B (Proxmox shell access). All four requirements share one SSH session
because each depends on the bus address captured by SPK-02. Sequential execution
within one session is mandatory — see 01-RESEARCH.md Pitfall 4 (lines 229-245).

`autonomous: false` — This plan opens an SSH session to the production Proxmox
host. The executor must have working SSH credentials before running it.

Output: 5 files — one raw SSH transcript + four per-SPK findings markdown files.
After completion, `task spk:verify` MUST exit 0 with all 14 rows passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-hardware-spike-discovery/01-RESEARCH.md
@.planning/phases/01-hardware-spike-discovery/01-VALIDATION.md
@Taskfile.yml
@CLAUDE.md
@CLAUDE.local.md

<interfaces>
Plan 03 prerequisite (hard dependency):
  Taskfile.yml MUST contain a working `spk:capture` target after Plan 03 commits.
  If `task --list | grep spk:capture` fails, STOP — Plan 03 is incomplete.

Proxmox host SSH target (from CLAUDE.local.md):
  Hostname: proxmox.ryanmcafee.com
  IP: 172.16.100.250
  User: root (existing workflow per env.hcl:124-125)

Expected lspci shape for the Intel Arc Pro B50 (from 01-RESEARCH.md line 283):
  XX:00.0 VGA compatible controller [0300]: Intel Corporation Arc Pro B50 [8086:e212]
          Subsystem: Intel Corporation Device [8086:XXXX]
          Kernel driver in use: (vfio-pci|xe|i915)
          Kernel modules: xe

Bus address format conversion:
  lspci short form: XX:00.0 (e.g. 03:00.0)
  sysfs long form:  0000:XX:00.0
  env.hcl expects:  0000:XX:00.0
  Findings MUST record the long form as `pci_bus_address`.

IOMMU isolation acceptance (from 01-RESEARCH.md line 291):
  VERIFIED: group contains ONLY the B50 main function (XX:00.0) and optionally its
    audio function (XX:00.1). Total devices <= 2.
  VERIFIED-WITH-FALLBACK: group contains 3+ devices; document pcie_acs_override
    workaround for Phase 7.
  BLOCKED: another discrete device (NIC, HBA) in the same group; surface to user.

vfio-pci binding (from 01-RESEARCH.md line 328):
  "Kernel driver in use: vfio-pci" -> ready for Phase 7 passthrough
  "xe" or "i915" -> NOT bound; Phase 7 needs driverctl override / modprobe blacklist

Findings file template (5 required headings):
  # SPK-NN: <description>
  **Status:** [VERIFIED | VERIFIED-WITH-FALLBACK | BLOCKED]
  **Captured:** YYYY-MM-DD HH:MM
  **Captured by:** <command>
  ## Command(s) executed
  ## Raw output
  ## Interpretation
  ## Value(s) extracted (for downstream phases)
  ## Fallback (if status != VERIFIED)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preflight — verify Proxmox SSH access and B50 physical presence</name>
  <files>.planning/phases/01-hardware-spike-discovery/findings/preflight.log</files>
  <read_first>
    - CLAUDE.local.md (Network Configuration section — Proxmox IP 172.16.100.250)
    - Taskfile.yml (confirm spk:capture target exists from Plan 03)
    - .planning/phases/01-hardware-spike-discovery/01-RESEARCH.md (lines 229-245 — Pitfall 4 — one-session transcript discipline)
  </read_first>
  <action>
Automate the Proxmox connectivity and B50 presence preflight checks BEFORE running
`task spk:capture` in Task 2. All four checks are read-only and scriptable via the
local shell. Tee the combined output to
`.planning/phases/01-hardware-spike-discovery/findings/preflight.log`.

Run each check sequentially; any non-zero exit or missing expected string MUST stop
the plan (do NOT proceed to Task 2):

Check 1 — Proxmox host reachable:

  ping -c 2 -W 2 proxmox.ryanmcafee.com 2>&1 | tee -a .planning/phases/01-hardware-spike-discovery/findings/preflight.log
  # Expected: "2 packets transmitted, 2 (packets )?received"

Check 2 — Plan 03 Taskfile target exists:

  task --list 2>&1 | grep -F 'spk:capture' | tee -a .planning/phases/01-hardware-spike-discovery/findings/preflight.log
  # Expected: line containing "spk:capture"
  # If empty, STOP: Plan 03 has not landed.

Check 3 — Non-interactive SSH works:

  ssh -o BatchMode=yes -o ConnectTimeout=5 root@proxmox.ryanmcafee.com 'echo PREFLIGHT_OK && hostname' 2>&1 | tee -a .planning/phases/01-hardware-spike-discovery/findings/preflight.log
  # Expected: "PREFLIGHT_OK" followed by the Proxmox hostname
  # If fails: fix ~/.ssh/config or add key before continuing. STOP.

Check 4 — Intel Arc Pro B50 visible to Proxmox kernel:

  ssh root@proxmox.ryanmcafee.com 'lspci -nn | grep -iE "intel.*(arc|e212)" || echo B50_NOT_FOUND' 2>&1 | tee -a .planning/phases/01-hardware-spike-discovery/findings/preflight.log
  # Expected: a single line containing "[8086:e212]"
  # If "B50_NOT_FOUND": the card is not seated, not powered, or BIOS-disabled. STOP and surface to user.

Append a trailing confirmation line:

  echo "PREFLIGHT_COMPLETE $(date +%Y-%m-%dT%H:%M:%S)" | tee -a .planning/phases/01-hardware-spike-discovery/findings/preflight.log

Pitfalls:
- Do NOT run destructive or state-changing commands. Every check here is read-only.
- Do NOT skip Check 2 — running `task spk:capture` before Plan 03 lands will fail with
  "task: task 'spk:capture' not found" and waste the SSH round-trip.
- Do NOT continue to Task 2 if any check fails. Surface the preflight.log contents to
  the user and stop the plan.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-hardware-spike-discovery/findings/preflight.log && grep -q 'PREFLIGHT_OK' .planning/phases/01-hardware-spike-discovery/findings/preflight.log && grep -qF 'spk:capture' .planning/phases/01-hardware-spike-discovery/findings/preflight.log && grep -qF '8086:e212' .planning/phases/01-hardware-spike-discovery/findings/preflight.log && grep -q 'PREFLIGHT_COMPLETE' .planning/phases/01-hardware-spike-discovery/findings/preflight.log && ! grep -q 'B50_NOT_FOUND' .planning/phases/01-hardware-spike-discovery/findings/preflight.log</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-hardware-spike-discovery/findings/preflight.log` exists
    - preflight.log contains the literal string `PREFLIGHT_OK` (Check 3 passed)
    - preflight.log contains the literal string `spk:capture` (Check 2 passed — Plan 03 landed)
    - preflight.log contains the literal string `8086:e212` (Check 4 passed — B50 visible)
    - preflight.log contains the literal string `PREFLIGHT_COMPLETE` followed by an ISO-8601 timestamp
    - preflight.log does NOT contain the literal string `B50_NOT_FOUND`
    - Every check exited 0; no non-zero exit codes propagated
  </acceptance_criteria>
  <done>All four preflight checks passed. Proxmox is reachable, non-interactive SSH works, the spk:capture target exists, and the B50 is visible at 8086:e212. Task 2 is safe to proceed.</done>
</task>

<task type="auto">
  <name>Task 2: Run task spk:capture against live Proxmox host</name>
  <files>.planning/phases/01-hardware-spike-discovery/findings/proxmox-ssh-session.log</files>
  <read_first>
    - .planning/phases/01-hardware-spike-discovery/01-RESEARCH.md (lines 440-478 — the exact Taskfile target this task invokes)
    - .planning/phases/01-hardware-spike-discovery/01-RESEARCH.md (lines 175-195 — Wave B sequential SSH session + anti-patterns)
    - Taskfile.yml (confirm spk:capture target exists after Plan 03)
    - CLAUDE.local.md (Network Configuration — Proxmox IP 172.16.100.250)
  </read_first>
  <action>
PRECHECK: Verify Plan 03 completed by running `task --list 2>/dev/null | grep -F spk:capture`.
If this fails, STOP — Plan 03 must land first.

Step 1 — Run the capture target:

  task spk:capture

This opens ONE SSH session to root@proxmox.ryanmcafee.com and runs the heredoc
(SPK-05 cat /proc/cmdline, SPK-02 lspci -nnk, SPK-04 lspci -vmmnn -s $BUS +
lspci -nnk -s $BUS, SPK-03 readlink + iommu_groups walk, SPK-05 bonus dmesg),
teeing the complete output to
`.planning/phases/01-hardware-spike-discovery/findings/proxmox-ssh-session.log`.
Expected runtime: ~10 seconds.

Step 2 — Sanity-check the transcript:

  test -f .planning/phases/01-hardware-spike-discovery/findings/proxmox-ssh-session.log
  grep -c '^=== SPK-0[2-5]' .planning/phases/01-hardware-spike-discovery/findings/proxmox-ssh-session.log
  # Expected: >= 4 (one header per SPK-02, 03, 04, 05; SPK-05 bonus dmesg is separate)
  grep -F '8086:e212' .planning/phases/01-hardware-spike-discovery/findings/proxmox-ssh-session.log
  grep -E '^=== Captured bus: [0-9a-f]{2}:[0-9a-f]{2}\.[0-9]' .planning/phases/01-hardware-spike-discovery/findings/proxmox-ssh-session.log

Step 3 — Extract the captured bus address for reference in later tasks:

  grep '^=== Captured bus:' .planning/phases/01-hardware-spike-discovery/findings/proxmox-ssh-session.log
  # Example output: === Captured bus: 03:00.0 ===  (the actual value depends on the live host)

If the transcript shows no match for `intel.*(arc|e212)` in SPK-02, STOP and surface
to the user — the B50 is not visible to the Proxmox kernel. The Task 1 checkpoint
should have caught this, so this branch is a safety net.

Pitfalls to avoid (from 01-RESEARCH.md lines 188-195, 218-227):
- Do NOT run the 4 commands individually via multiple ssh invocations — that breaks
  the reproducible one-session guarantee.
- Do NOT hand-edit the transcript. It is the raw source of truth for tasks 3-5.
- Do NOT trust the RESEARCH.md example bus address `03:00.0`. The live host may
  report a different slot. Always use the live-captured value.
- Do NOT issue any state-changing commands inside the SSH session. task spk:capture
  is read-only by design.

Delegate to the `debugger` subagent ONLY if `task spk:capture` hangs or fails.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-hardware-spike-discovery/findings/proxmox-ssh-session.log && test "$(grep -c '^=== SPK-0[2-5]' .planning/phases/01-hardware-spike-discovery/findings/proxmox-ssh-session.log)" -ge 4 && grep -q '8086:e212' .planning/phases/01-hardware-spike-discovery/findings/proxmox-ssh-session.log && grep -qE '^=== Captured bus: [0-9a-f]{2}:[0-9a-f]{2}\.[0-9]' .planning/phases/01-hardware-spike-discovery/findings/proxmox-ssh-session.log && grep -q 'intel_iommu=on' .planning/phases/01-hardware-spike-discovery/findings/proxmox-ssh-session.log</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-hardware-spike-discovery/findings/proxmox-ssh-session.log` exists and is non-empty
    - Transcript contains at least 4 section headers matching `^=== SPK-0[2-5]` (one per Wave B requirement)
    - Transcript contains the literal substring `8086:e212` (proves the B50 is enumerated by the Proxmox kernel)
    - Transcript contains a line matching `^=== Captured bus: [0-9a-f]{2}:[0-9a-f]{2}\.[0-9]` (real short-form bus address)
    - Transcript contains the literal substring `intel_iommu=on` (from SPK-05 /proc/cmdline)
    - Transcript contains a line matching `^IOMMU group: [0-9]+` (from SPK-03 sysfs walk)
    - Transcript contains a `Kernel driver in use:` line (from SPK-04 lspci -nnk)
    - `task spk:capture` exited 0 (SSH session ran cleanly)
  </acceptance_criteria>
  <done>proxmox-ssh-session.log has all 4 SPK sections populated. The B50 is visible at 8086:e212, a bus address is captured, an IOMMU group is assigned, and the kernel driver binding is recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Author SPK-02 (pci-id) and SPK-05 (iommu-cmdline) findings files</name>
  <files>
    .planning/phases/01-hardware-spike-discovery/findings/spk-02-pci-id.md,
    .planning/phases/01-hardware-spike-discovery/findings/spk-05-iommu-cmdline.md
  </files>
  <read_first>
    - .planning/phases/01-hardware-spike-discovery/findings/proxmox-ssh-session.log (Task 2 output)
    - .planning/phases/01-hardware-spike-discovery/01-RESEARCH.md (lines 277-292 — SPK-02 expected output)
    - .planning/phases/01-hardware-spike-discovery/01-RESEARCH.md (lines 340-350 — SPK-05)
    - .planning/phases/01-hardware-spike-discovery/01-RESEARCH.md (lines 131-161 — findings template)
    - .planning/phases/01-hardware-spike-discovery/01-VALIDATION.md (rows 1-B-01, 1-B-02, 1-B-07)
  </read_first>
  <action>
Step 1 — Extract the bus address from the transcript:

  BUS_SHORT=$(grep '^=== Captured bus:' .planning/phases/01-hardware-spike-discovery/findings/proxmox-ssh-session.log | awk '{print $4}')
  BUS_LONG="0000:${BUS_SHORT}"
  CAPTURED_TS=$(date +"%Y-%m-%d %H:%M")

If BUS_SHORT is empty, STOP. Do NOT invent a value.

Step 2 — Write `findings/spk-02-pci-id.md` using the template from 01-RESEARCH.md
lines 131-161. Required content:

- Title: `# SPK-02: Intel Arc Pro B50 PCI bus address captured from live Proxmox host`
- `**Status:**` MUST be `VERIFIED` if `8086:e212` is in the transcript, else `BLOCKED`
- `**Captured:**` MUST be the real `$CAPTURED_TS` value
- `**Captured by:**` MUST be `task spk:capture (ssh root@proxmox.ryanmcafee.com lspci -nnk)`
- `## Command(s) executed` fence MUST contain the literal bash command:
  `ssh root@proxmox.ryanmcafee.com "lspci -nnk | grep -A 3 -iE 'intel.*(arc|e212)'"`
- `## Raw output` fence MUST contain the verbatim SPK-02 section from the transcript
  (everything between `=== SPK-02: B50 PCI ID ===` and the next `===` marker)
- `## Interpretation` block MUST state: "The Intel Arc Pro B50 is visible at bus `$BUS_LONG`
  (short form `$BUS_SHORT`) with vendor:device `8086:e212`, matching the Intel ARK reference.
  The card is powered, enumerated by the Proxmox kernel, and ready for SPK-03 IOMMU
  enumeration and SPK-04 subsystem ID capture."
- `## Value(s) extracted (for downstream phases)` MUST contain these exact bullet lines:
  - `- pci_bus_address: 0000:XX:00.0` (substitute real long-form value)
  - `- pci_bus_address_short: XX:00.0` (substitute real short-form value)
  - `- vendor_device: 8086:e212`
  - `- kernel_modules: xe` (or actual value from the transcript)
- `## Fallback (if status != VERIFIED)` MUST state: "If `8086:e212` is absent, the B50
  is either not physically seated, not powered, or the BIOS has disabled its PCIe slot.
  Phase 1 cannot proceed. Inspect hardware, reseat the card, verify BIOS PCIe slot
  settings, and re-run `task spk:capture`."

Step 3 — Write `findings/spk-05-iommu-cmdline.md`:

- Title: `# SPK-05: Proxmox host kernel cmdline contains intel_iommu=on`
- `**Status:**` MUST be `VERIFIED` if `intel_iommu=on` is in the transcript, else `BLOCKED`
- `**Captured:**` MUST be the real `$CAPTURED_TS` value
- `**Captured by:**` MUST be `task spk:capture (ssh root@proxmox.ryanmcafee.com cat /proc/cmdline)`
- `## Command(s) executed` fence MUST contain:
  `ssh root@proxmox.ryanmcafee.com 'cat /proc/cmdline'`
  `ssh root@proxmox.ryanmcafee.com 'dmesg | grep -i "DMAR.*IOMMU enabled" | head -5'`
- `## Raw output` fence MUST contain the verbatim SPK-05 and SPK-05 bonus dmesg sections
- `## Interpretation` block MUST state verbatim: "The Proxmox host kernel cmdline contains
  `intel_iommu=on`, confirming Intel VT-d is enabled. This was already true prior to
  Phase 1 because the existing NVIDIA Quadro P2200 passthrough on worker-1 depends on
  it. The dmesg line `DMAR: IOMMU enabled` corroborates the runtime state. No changes
  required for Intel Arc Pro B50 passthrough."
- `## Value(s) extracted (for downstream phases)` MUST contain:
  - `- intel_iommu: on`
  - `- iommu_pt: yes` or `- iommu_pt: no` (based on whether iommu=pt appears)
  - `- dmar_enabled_dmesg: yes` or `- dmar_enabled_dmesg: no`
- `## Fallback (if status != VERIFIED)` MUST state: "If intel_iommu=on is absent, update
  Proxmox /etc/default/grub GRUB_CMDLINE_LINUX_DEFAULT to add `intel_iommu=on iommu=pt`,
  run update-grub, and reboot. This MUST be done before Phase 7 HW-07. However, this
  outcome is unexpected because NVIDIA passthrough already works on this host."

Pitfalls:
- Do NOT fabricate a bus address if extraction fails. STOP and surface the transcript.
- Do NOT use RESEARCH.md example value 03:00.0 unless the live transcript shows it.
- Do NOT omit the `kernel_modules` bullet in spk-02 — it's the SPK-01 cross-check.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-hardware-spike-discovery/findings/spk-02-pci-id.md && test -f .planning/phases/01-hardware-spike-discovery/findings/spk-05-iommu-cmdline.md && grep -E '^- pci_bus_address: 0000:[0-9a-f]{2}:[0-9a-f]{2}\.[0-9]$' .planning/phases/01-hardware-spike-discovery/findings/spk-02-pci-id.md && grep -F '8086:e212' .planning/phases/01-hardware-spike-discovery/findings/spk-02-pci-id.md && grep -E '^\*\*Status:\*\* (VERIFIED|BLOCKED)$' .planning/phases/01-hardware-spike-discovery/findings/spk-02-pci-id.md && grep -F 'intel_iommu=on' .planning/phases/01-hardware-spike-discovery/findings/spk-05-iommu-cmdline.md && grep -E '^\*\*Status:\*\* (VERIFIED|BLOCKED)$' .planning/phases/01-hardware-spike-discovery/findings/spk-05-iommu-cmdline.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-hardware-spike-discovery/findings/spk-02-pci-id.md` exists
    - spk-02 contains exact literal `**Status:** VERIFIED` OR `**Status:** BLOCKED` on its own line
    - spk-02 contains a line matching regex `^- pci_bus_address: 0000:[0-9a-f]{2}:[0-9a-f]{2}\.[0-9]$` (01-VALIDATION.md row 1-B-01)
    - spk-02 contains the literal substring `8086:e212` (01-VALIDATION.md row 1-B-02)
    - spk-02 contains the literal string `- pci_bus_address_short:`
    - spk-02 contains the literal string `- vendor_device: 8086:e212`
    - spk-02 contains the literal string `- kernel_modules:`
    - spk-02 contains all 5 template headings (Command(s) executed, Raw output, Interpretation, Value(s) extracted, Fallback)
    - `.planning/phases/01-hardware-spike-discovery/findings/spk-05-iommu-cmdline.md` exists
    - spk-05 contains exact literal `**Status:** VERIFIED` OR `**Status:** BLOCKED` on its own line
    - spk-05 contains the literal substring `intel_iommu=on` (01-VALIDATION.md row 1-B-07)
    - spk-05 contains the literal string `- intel_iommu: on`
    - spk-05 contains the literal string `- iommu_pt:`
    - spk-05 contains the literal string `- dmar_enabled_dmesg:`
    - spk-05 contains all 5 template headings
    - Neither file contains literal "TODO" or "placeholder" strings
  </acceptance_criteria>
  <done>spk-02-pci-id.md has the real captured long-form bus address and vendor_device 8086:e212. spk-05-iommu-cmdline.md has intel_iommu=on confirmation. Both files pass 01-VALIDATION.md rows 1-B-01, 1-B-02, 1-B-07.</done>
</task>

<task type="auto">
  <name>Task 4: Author SPK-04 (subsystem-id) findings file with vfio-pci binding state</name>
  <files>.planning/phases/01-hardware-spike-discovery/findings/spk-04-subsystem-id.md</files>
  <read_first>
    - .planning/phases/01-hardware-spike-discovery/findings/proxmox-ssh-session.log (SPK-04 section)
    - .planning/phases/01-hardware-spike-discovery/findings/spk-02-pci-id.md (Task 3 output — bus reference)
    - .planning/phases/01-hardware-spike-discovery/01-RESEARCH.md (lines 315-338 — SPK-04 expected output)
    - .planning/phases/01-hardware-spike-discovery/01-RESEARCH.md (lines 131-161 — findings template)
    - .planning/phases/01-hardware-spike-discovery/01-VALIDATION.md (rows 1-B-05, 1-B-06)
  </read_first>
  <action>
Step 1 — Extract SVendor, SDevice, and Kernel driver in use from the transcript:

  SECTION=$(awk '/^=== SPK-04: subsystem ID/,/^=== SPK-03:/' .planning/phases/01-hardware-spike-discovery/findings/proxmox-ssh-session.log)
  SVENDOR=$(echo "$SECTION" | grep -E '^SVendor:' | sed -E 's/.*\[([0-9a-f]{4})\]$/\1/')
  SDEVICE=$(echo "$SECTION" | grep -E '^SDevice:' | sed -E 's/.*\[([0-9a-f]{4})\]$/\1/')
  SUBSYSTEM_ID="${SVENDOR}:${SDEVICE}"
  DRIVER=$(echo "$SECTION" | grep -E 'Kernel driver in use:' | awk -F': ' '{print $2}' | tr -d ' ')

If SVENDOR or SDEVICE extraction fails, STOP and surface the raw transcript section.

Step 2 — Determine Status based on driver binding:

  case "$DRIVER" in
    vfio-pci) STATUS=VERIFIED ;;
    xe|i915)  STATUS=VERIFIED-WITH-FALLBACK ;;
    "")       STATUS=BLOCKED ;;
    *)        STATUS=VERIFIED-WITH-FALLBACK ;;
  esac

Step 3 — Write `findings/spk-04-subsystem-id.md`:

- Title: `# SPK-04: Intel Arc Pro B50 subsystem ID and vfio-pci binding state`
- `**Status:**` MUST be the computed `$STATUS` value
- `**Captured:**` MUST be `$(date +"%Y-%m-%d %H:%M")`
- `**Captured by:**` MUST be `task spk:capture (ssh root@proxmox.ryanmcafee.com lspci -vmmnn + lspci -nnk)`
- `## Command(s) executed` fence MUST contain the bash commands:
  `ssh root@proxmox.ryanmcafee.com 'lspci -vmmnn -s $BUS'`
  `ssh root@proxmox.ryanmcafee.com 'lspci -nnk -s $BUS'`
- `## Raw output` fence MUST contain the verbatim SPK-04 section from the transcript
- `## Interpretation` branches on $STATUS:
  * VERIFIED: "Subsystem ID `$SUBSYSTEM_ID`. The B50 is bound to `vfio-pci`, which is
    the required driver for PCI passthrough. Phase 5 terragrunt env.hcl Intel block
    can use this subsystem ID directly. Phase 7 hardware bring-up needs no
    driver-unbinding preparation."
  * VERIFIED-WITH-FALLBACK: "Subsystem ID `$SUBSYSTEM_ID`. Currently bound to `$DRIVER`
    (not vfio-pci). Expected if the Proxmox host has not yet been configured for B50
    passthrough (it previously ran NVIDIA passthrough only). Phase 7 MUST add a
    driverctl override or modprobe blacklist step before `talos:recreate:node worker-1`
    runs. Phase 5 env.hcl values are still valid; only the binding sequence changes."
  * BLOCKED: "No kernel driver bound. `lspci -nnk` is missing a `Kernel driver in use:`
    line entirely. Check `/sys/bus/pci/devices/0000:${BUS_LONG}/`, dmesg for e212
    errors, and Proxmox syslog. Phase 7 cannot proceed."
- `## Value(s) extracted (for downstream phases)` MUST contain:
  - `- subsystem_id: XXXX:XXXX` (substitute real value; 01-VALIDATION.md row 1-B-05)
  - `- svendor: XXXX`
  - `- sdevice: XXXX`
  - `- kernel_driver_in_use: vfio-pci` (or actual driver name)
  - `- phase_7_binding_prep_required: no` (or `yes` if not vfio-pci)
- `## Fallback (if status != VERIFIED)` MUST state: "For VERIFIED-WITH-FALLBACK: add a
  `driverctl set-override $BUS_LONG vfio-pci` step (or modprobe blacklist in
  /etc/modprobe.d/) to Phase 7 HW-07 prerequisites. For BLOCKED: investigate sysfs
  state via `/sys/bus/pci/devices/`, `dmesg | grep -i 'e212\|arc'`, and syslog."

Pitfalls:
- Do NOT fabricate a subsystem ID. Surface empty extraction to the user.
- Do NOT assume `8086:XXXX` — Sparkle/ASRock cards may use their own SVendor (01-RESEARCH.md line 69).
- Do NOT mark Status=VERIFIED if the driver is not vfio-pci. Phase 7 needs advance warning.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-hardware-spike-discovery/findings/spk-04-subsystem-id.md && grep -E '^- subsystem_id: [0-9a-f]{4}:[0-9a-f]{4}$' .planning/phases/01-hardware-spike-discovery/findings/spk-04-subsystem-id.md && grep -iE 'vfio-pci|kernel driver' .planning/phases/01-hardware-spike-discovery/findings/spk-04-subsystem-id.md && grep -E '^\*\*Status:\*\* (VERIFIED|VERIFIED-WITH-FALLBACK|BLOCKED)$' .planning/phases/01-hardware-spike-discovery/findings/spk-04-subsystem-id.md && grep -E '^- kernel_driver_in_use:' .planning/phases/01-hardware-spike-discovery/findings/spk-04-subsystem-id.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-hardware-spike-discovery/findings/spk-04-subsystem-id.md` exists
    - File contains exact literal `**Status:** VERIFIED` OR `**Status:** VERIFIED-WITH-FALLBACK` OR `**Status:** BLOCKED`
    - File contains a line matching regex `^- subsystem_id: [0-9a-f]{4}:[0-9a-f]{4}$` (01-VALIDATION.md row 1-B-05)
    - File contains a case-insensitive match for `vfio-pci` OR `kernel driver` (01-VALIDATION.md row 1-B-06)
    - File contains a line matching `^- kernel_driver_in_use:`
    - File contains the literal string `- svendor:`
    - File contains the literal string `- sdevice:`
    - File contains the literal string `- phase_7_binding_prep_required:`
    - File contains all 5 template headings
    - File does NOT contain literal "TODO" or "placeholder" strings
  </acceptance_criteria>
  <done>spk-04-subsystem-id.md has the real captured subsystem ID, current kernel driver binding, and a Status field that tells Phase 7 whether driver-unbinding prep is needed.</done>
</task>

<task type="auto">
  <name>Task 5: Author SPK-03 (iommu-group) findings file with isolation verdict</name>
  <files>.planning/phases/01-hardware-spike-discovery/findings/spk-03-iommu-group.md</files>
  <read_first>
    - .planning/phases/01-hardware-spike-discovery/findings/proxmox-ssh-session.log (SPK-03 section)
    - .planning/phases/01-hardware-spike-discovery/findings/spk-02-pci-id.md (bus reference)
    - .planning/phases/01-hardware-spike-discovery/01-RESEARCH.md (lines 294-313 — SPK-03 expected output)
    - .planning/phases/01-hardware-spike-discovery/01-RESEARCH.md (lines 218-227 — Pitfall 2: IOMMU prediction)
    - .planning/phases/01-hardware-spike-discovery/01-RESEARCH.md (lines 131-161 — findings template)
    - .planning/phases/01-hardware-spike-discovery/01-VALIDATION.md (rows 1-B-03, 1-B-04)
  </read_first>
  <action>
Step 1 — Extract IOMMU group number and device list from the transcript:

  SECTION=$(awk '/^=== SPK-03: IOMMU group/,/^=== SPK-05 \(bonus\)/' .planning/phases/01-hardware-spike-discovery/findings/proxmox-ssh-session.log)
  GROUP_NUM=$(echo "$SECTION" | grep -E '^IOMMU group:' | awk '{print $3}')
  DEVICE_LINES=$(echo "$SECTION" | awk '/^Devices in group/,0' | tail -n +2 | grep -v '^$' | grep -v '^===')
  DEVICE_COUNT=$(echo "$DEVICE_LINES" | wc -l | tr -d ' ')

Step 2 — Apply isolation decision logic (01-RESEARCH.md line 291):

  - Count only non-B50 devices in the group (B50 is XX:00.0 and optionally XX:00.1 audio)
  - If DEVICE_COUNT <= 2 AND all devices are B50 functions -> STATUS=VERIFIED
  - If DEVICE_COUNT >= 3 AND extra devices are chipset/PCH/root-port only -> STATUS=VERIFIED-WITH-FALLBACK
    (workaround: pcie_acs_override=downstream,multifunction cmdline arg)
  - If DEVICE_COUNT >= 3 AND an extra device is a discrete NIC/HBA/storage -> STATUS=BLOCKED
    (surface to user: requires hardware reseating or BIOS ACS override)

Step 3 — Write `findings/spk-03-iommu-group.md`:

- Title: `# SPK-03: Intel Arc Pro B50 IOMMU group membership and isolation status`
- `**Status:**` MUST be the computed `$STATUS`
- `**Captured:**` MUST be `$(date +"%Y-%m-%d %H:%M")`
- `**Captured by:**` MUST be `task spk:capture (ssh root@proxmox.ryanmcafee.com sysfs walk /sys/kernel/iommu_groups)`
- `## Command(s) executed` fence MUST contain the bash commands:
  `ssh root@proxmox.ryanmcafee.com 'readlink /sys/bus/pci/devices/0000:${BUS}/iommu_group'`
  `ssh root@proxmox.ryanmcafee.com 'for dev in /sys/kernel/iommu_groups/$GROUP/devices/*; do lspci -nns $(basename $dev); done'`
- `## Raw output` fence MUST contain the verbatim SPK-03 section from the transcript
- `## Interpretation` branches on $STATUS:
  * VERIFIED: "The Intel Arc Pro B50 is in IOMMU group `$GROUP_NUM`, which contains
    only the B50 main function (and optionally its audio function). Isolation is
    clean — no workaround required. Phase 7 passthrough will succeed without
    pcie_acs_override or hardware reseating."
  * VERIFIED-WITH-FALLBACK: "The Intel Arc Pro B50 is in IOMMU group `$GROUP_NUM`,
    which also contains chipset/PCH/root-port devices but no discrete peripherals.
    Phase 7 MUST add `pcie_acs_override=downstream,multifunction` to the Proxmox
    host kernel cmdline before `talos:recreate:node worker-1` runs. Document this
    as a Phase 7 HW-07 prerequisite."
  * BLOCKED: "The Intel Arc Pro B50 shares IOMMU group `$GROUP_NUM` with another
    discrete device. pcie_acs_override is not a safe workaround for this case —
    the shared device would be forced into the same VFIO container. Phase 7
    cannot proceed without either hardware reseating the B50 into a different
    PCIe slot (ideally a CPU-attached slot, not chipset-attached) OR enabling
    motherboard-level BIOS ACS if supported. Surface this finding to the user
    immediately; this is a blocker for the entire GPU vendor migration."
- `## Value(s) extracted (for downstream phases)` MUST contain:
  - `- iommu_group: $GROUP_NUM` (01-VALIDATION.md row 1-B-03)
  - `- devices_in_group_count: $DEVICE_COUNT`
  - `- devices_in_group: <one-line comma-separated list of pci addresses>`
  - `- isolation_workaround_required: <none|pcie_acs_override|hardware_reseat>`
- `## Fallback (if status != VERIFIED)` MUST state: "For VERIFIED-WITH-FALLBACK: add
  `pcie_acs_override=downstream,multifunction` to Proxmox /etc/default/grub
  GRUB_CMDLINE_LINUX_DEFAULT, run update-grub, reboot, and re-run `task spk:capture`
  to confirm the group now contains only B50 functions. Document the override as a
  Phase 7 HW-07 prerequisite. For BLOCKED: hardware reseating is required; do NOT
  attempt pcie_acs_override with another discrete device in the same group."

Pitfalls (from 01-RESEARCH.md lines 218-227):
- Do NOT assume IOMMU groups from datasheets. The result is motherboard-specific.
- Do NOT use pcie_acs_override as a blanket workaround — if another discrete device
  (NIC, HBA) is in the group, it creates a security/stability problem.
- Do NOT mark VERIFIED if any non-B50 device is in the group.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-hardware-spike-discovery/findings/spk-03-iommu-group.md && grep -E '^- iommu_group: [0-9]+$' .planning/phases/01-hardware-spike-discovery/findings/spk-03-iommu-group.md && grep -E '^\*\*Status:\*\* (VERIFIED|VERIFIED-WITH-FALLBACK|BLOCKED)$' .planning/phases/01-hardware-spike-discovery/findings/spk-03-iommu-group.md && grep -E '^- devices_in_group_count:' .planning/phases/01-hardware-spike-discovery/findings/spk-03-iommu-group.md && grep -E '^- isolation_workaround_required:' .planning/phases/01-hardware-spike-discovery/findings/spk-03-iommu-group.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-hardware-spike-discovery/findings/spk-03-iommu-group.md` exists
    - File contains exact literal `**Status:** VERIFIED` OR `**Status:** VERIFIED-WITH-FALLBACK` OR `**Status:** BLOCKED` (01-VALIDATION.md row 1-B-04)
    - File contains a line matching regex `^- iommu_group: [0-9]+$` (01-VALIDATION.md row 1-B-03)
    - File contains the literal string `- devices_in_group_count:`
    - File contains the literal string `- devices_in_group:`
    - File contains the literal string `- isolation_workaround_required:` followed by `none`, `pcie_acs_override`, or `hardware_reseat`
    - File contains all 5 template headings
    - File does NOT contain literal "TODO" or "placeholder" strings
  </acceptance_criteria>
  <done>spk-03-iommu-group.md has the real captured IOMMU group number, the full device list, and an isolation status field that tells Phase 7 whether a workaround or hardware reseat is required.</done>
</task>

<task type="auto">
  <name>Task 6: Rollup — run task spk:verify and confirm all 14 validation rows pass</name>
  <files>.planning/phases/01-hardware-spike-discovery/findings/spk-verify-rollup.txt</files>
  <read_first>
    - .planning/phases/01-hardware-spike-discovery/01-VALIDATION.md (lines 36-58 — Per-Task Verification Map)
    - Taskfile.yml (confirm spk:verify target from Plan 03)
  </read_first>
  <action>
Step 1 — Run the verify target and capture its output:

  task spk:verify 2>&1 | tee .planning/phases/01-hardware-spike-discovery/findings/spk-verify-rollup.txt

Expected output: 14 lines matching `^PASS  1-[AB]-0[1-7]$` plus a summary line
matching `Passed: 14  Failed: 0`.

Step 2 — Check the exit code:

  task spk:verify
  echo "exit_code=$?"

Expected: exit_code=0. If non-zero, identify which rows failed, re-inspect the
corresponding findings file, and fix the extraction before proceeding.

Step 3 — Confirm the reproducibility criterion from 01-VALIDATION.md row 1-C-01
("task spk:capture re-runs cleanly; transcript diffs only on timestamps"):

  cp .planning/phases/01-hardware-spike-discovery/findings/proxmox-ssh-session.log /tmp/spk-capture-run1.log
  task spk:capture
  diff /tmp/spk-capture-run1.log .planning/phases/01-hardware-spike-discovery/findings/proxmox-ssh-session.log | \
    grep -vE '(timestamp|^[0-9]+[acd][0-9]+$|^---$|^> $|^< $)' || echo "No semantic diff — reproducibility OK"

If the diff shows PCI IDs or IOMMU groups changing between captures, STOP — hardware
state is unstable and Phase 5/7 cannot rely on Phase 1 evidence.

Step 4 — Final rollup check (must pass for Phase 1 to be complete):

  task spk:verify && echo "Phase 1 Wave B complete — all 14 validation rows green"

Do NOT modify the findings files in this task — the rollup is read-only. If any row
fails, return to Tasks 3/4/5 to fix the corresponding file; do not patch the
findings file to satisfy the grep without re-running `task spk:capture`.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-hardware-spike-discovery/findings/spk-verify-rollup.txt && grep -q 'Passed: 14  Failed: 0' .planning/phases/01-hardware-spike-discovery/findings/spk-verify-rollup.txt && task spk:verify</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-hardware-spike-discovery/findings/spk-verify-rollup.txt` exists
    - Rollup file contains the literal string `Passed: 14  Failed: 0`
    - Rollup file contains 14 lines matching `^PASS  1-[AB]-0[1-7]$`
    - Rollup file contains no lines matching `^FAIL `
    - `task spk:verify` exits 0
    - `task spk:capture` re-run produces no semantic diff from the previous transcript (reproducibility check — satisfies 01-VALIDATION.md row 1-C-01)
  </acceptance_criteria>
  <done>All 14 validation rows from 01-VALIDATION.md pass. `task spk:verify` exits 0. The capture is reproducible (re-running shows no semantic diff). Phase 1 is complete and Phase 2 can begin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine -> Proxmox host | SSH session from operator workstation to root@proxmox.ryanmcafee.com (172.16.100.250) — existing trusted operator path per env.hcl:124-125 |
| Proxmox host -> sysfs / /proc | Read-only inspection of /proc/cmdline, /sys/bus/pci, /sys/kernel/iommu_groups — no mutation |
| Proxmox host -> stdout -> developer filesystem | Transcript streamed back over SSH, teed into gitignored findings directory |
| developer machine -> local filesystem | Findings files written under `.planning/` (gitignored; PII-safe) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Spoofing | SSH to Proxmox host | mitigate | SSH uses existing operator known_hosts and keypair. StrictHostKeyChecking is enabled by default (no -o StrictHostKeyChecking=no in the heredoc). Host key pinning catches a MITM between operator and Proxmox host. |
| T-01-04-02 | Tampering | Proxmox host state | mitigate | Every command in spk:capture is read-only (cat, lspci, readlink, basename, dmesg | grep). No state-changing commands. set -euo pipefail aborts the session on any non-zero exit. No sudo or su escalation. |
| T-01-04-03 | Information Disclosure | findings/proxmox-ssh-session.log | mitigate | `.planning/` is gitignored (verified via `git status --porcelain` returning empty for the findings dir). PCI IDs, IOMMU groups, and subsystem IDs are environment fingerprints — never committed. No secrets captured (the session does not read /etc/shadow, /root/.ssh, SOPS keys, or 1Password token). |
| T-01-04-04 | Information Disclosure | Terminal scrollback | accept | The operator's terminal scrollback may contain the transcript. Standard operator hygiene applies; this is not a deployment surface. |
| T-01-04-05 | Denial of Service | Proxmox host | accept | The session consists of <10 lspci invocations and a short dmesg grep. Negligible CPU/IO load. No concurrent sessions. |
| T-01-04-06 | Elevation of Privilege | SSH session | mitigate | Session runs as root (existing operator convention), but executes only read-only commands. The heredoc is source-controlled in Taskfile.yml so any privilege misuse would show up in code review. |
| T-01-04-07 | Repudiation | Captured evidence | mitigate | Every finding file records the exact command and timestamp. Reproducibility via `task spk:capture` re-run is the integrity control. |
| T-01-04-08 | Supply Chain | lspci output format | accept | pciutils is a Debian-packaged tool on the Proxmox host; output format is stable across versions. The grep patterns in spk:verify tolerate minor whitespace variation. |
</threat_model>

<verification>
After all tasks complete, run:
  task spk:verify
  # Expected: exit 0, 14 PASS lines, "Passed: 14  Failed: 0" summary

  test -f .planning/phases/01-hardware-spike-discovery/findings/proxmox-ssh-session.log
  test -f .planning/phases/01-hardware-spike-discovery/findings/spk-02-pci-id.md
  test -f .planning/phases/01-hardware-spike-discovery/findings/spk-03-iommu-group.md
  test -f .planning/phases/01-hardware-spike-discovery/findings/spk-04-subsystem-id.md
  test -f .planning/phases/01-hardware-spike-discovery/findings/spk-05-iommu-cmdline.md

  # Reproducibility (01-VALIDATION.md row 1-C-01):
  cp .planning/phases/01-hardware-spike-discovery/findings/proxmox-ssh-session.log /tmp/spk-run1.log
  task spk:capture
  diff /tmp/spk-run1.log .planning/phases/01-hardware-spike-discovery/findings/proxmox-ssh-session.log
  # Expected: only timestamp/runtime differences, NO PCI/IOMMU/driver value drift

  # Confirm findings stay gitignored:
  git status --porcelain .planning/phases/01-hardware-spike-discovery/findings/
  # Expected: EMPTY
</verification>

<success_criteria>
- [ ] proxmox-ssh-session.log exists and contains 4+ `=== SPK-0[2-5]` section headers
- [ ] spk-02-pci-id.md: VERIFIED status, real `pci_bus_address: 0000:XX:00.0`, `8086:e212`
- [ ] spk-03-iommu-group.md: VERIFIED|VERIFIED-WITH-FALLBACK|BLOCKED status, real `iommu_group:`, device list, isolation workaround field
- [ ] spk-04-subsystem-id.md: VERIFIED|VERIFIED-WITH-FALLBACK|BLOCKED status, real `subsystem_id:`, kernel_driver_in_use, phase_7_binding_prep_required
- [ ] spk-05-iommu-cmdline.md: VERIFIED status, `intel_iommu=on`, iommu_pt, dmar_enabled_dmesg
- [ ] `task spk:verify` exits 0 with "Passed: 14  Failed: 0"
- [ ] spk-verify-rollup.txt captures the verify output
- [ ] `task spk:capture` re-run produces no semantic diff (reproducibility — row 1-C-01)
- [ ] `.planning/` findings directory stays out of git (`git status --porcelain` empty)
- [ ] No Deno TS helper scripts created; all command execution via Taskfile `spk:*` targets
- [ ] No `curl` usage (CLAUDE.md compliance)
- [ ] All SSH commands were read-only; no state-changing commands were run on Proxmox
</success_criteria>

<output>
After completion, create `.planning/phases/01-hardware-spike-discovery/01-04-SUMMARY.md`
summarizing:
  (a) The real B50 bus address (long form) captured from the live Proxmox host
  (b) The real subsystem ID (SVendor:SDevice) and current kernel driver binding
  (c) The IOMMU group number and isolation verdict (VERIFIED / FALLBACK / BLOCKED)
  (d) The `intel_iommu=on` confirmation from /proc/cmdline
  (e) Any Phase 7 HW-07 prerequisites surfaced by Status != VERIFIED (pcie_acs_override,
      driverctl override, hardware reseating)
  (f) Reference to the proxmox-ssh-session.log transcript as the raw source of truth
  (g) `task spk:verify` rollup — 14/14 rows green, Phase 1 complete
</output>
