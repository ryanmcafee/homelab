# expect: Healthy
# message: No generation information
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: traefik-external
  namespace: traefik
  generation: 1
spec:
  endpoints:
    - dnsName: plex.homelab.local
      recordType: A
      recordTTL: 300
      targets:
        - 172.16.100.201
