# expect: ingressroute-class
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: loaded-by-nobody
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`auth.example.com`)
      services:
        - kind: TraefikService
          name: noop@internal
