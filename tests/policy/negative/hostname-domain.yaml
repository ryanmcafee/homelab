# expect: hostname-domain
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: broken-route
  namespace: default
spec:
  parentRefs:
    - name: envoy-internal
      namespace: envoy-gateway-system
      sectionName: https
  hostnames:
    - app.wrong-domain.com
  rules:
    - backendRefs:
        - name: app
          port: 80
