# expect: httproute-target
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: plex
  namespace: plex
  annotations:
    external-dns.alpha.kubernetes.io/hostname: plex.example.com
    external-dns.alpha.kubernetes.io/target: lab.duckdns.org
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: envoy-external
      namespace: envoy-gateway-system
      sectionName: https
  hostnames:
    - plex.example.com
  rules:
    - backendRefs:
        - name: plex
          port: 32400
